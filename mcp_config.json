{
  "mcpServers": {
    "solinfinte-alpaca": {
      "command": "python",
      "args": ["alpaca_cli.py"],
      "env": {
        "ALPACA_API_KEY": "${ALPACA_API_KEY}",
        "ALPACA_SECRET_KEY": "${ALPACA_SECRET_KEY}",
        "ALPACA_PAPER": "true"
      },
      "description": "Lets AI assistants (Claude, Cursor, VS Code, Antigravity) interact with Alpaca's paper trading environment through structured tools."
    }
  }
}
